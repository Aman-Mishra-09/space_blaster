services:
  - type: web
    name: space-blaster
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd space_blaster && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0
